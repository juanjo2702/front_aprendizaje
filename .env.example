# Frontend Environment Variables (Development)
# Copy to .env.local and adjust as needed

# Backend API URL (debe coincidir con la URL del backend)
VITE_API_BACKEND_URL=http://localhost:8000
VITE_API_URL=/api
VITE_APP_ENV=development
