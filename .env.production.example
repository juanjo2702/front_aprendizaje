# Frontend Environment Variables (Production)
# Estas variables se inyectan en tiempo de build (docker build)

# Backend API URL (producción)
VITE_API_BACKEND_URL=https://api.yourdomain.com
VITE_API_URL=/api
VITE_APP_ENV=production
